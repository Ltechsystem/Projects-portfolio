
  # Portfolio page with projects

  This is a code bundle for Portfolio page with projects. The original project is available at https://www.figma.com/design/ctMDAa47tCrVesagN9kObM/Portfolio-page-with-projects.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  